const sql = require('mssql'); require('dotenv').config(); const dbConfig = { user: process.env.DB_USER, password: process.env.DB_PASSWORD, server: process.env.DB_SERVER || process.env.DB_HOST, database: process.env.DB_NAME, options: { encrypt: false, trustServerCertificate: true } }; async function test() { try { const pool = await sql.connect(dbConfig); const r1 = await pool.request().query('SELECT TOP 1 * FROM dbo.BARCODE'); const r2 = await pool.request().query('SELECT TOP 1 * FROM dbo.HD_ITEMMASTER'); require('fs').writeFileSync('db_schema.json', JSON.stringify({ barcode: Object.keys(r1.recordset[0]), itemmaster: Object.keys(r2.recordset[0]) }, null, 2)); pool.close(); } catch (e) { console.error(e); } } test();
